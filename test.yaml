
# ===== 基础设置 =====
port: 7890
socks-port: 7891
allow-lan: false
mode: rule
log-level: info
external-controller: 127.0.0.1:9090
secret: "your_password"  # 修改为自定义密码

# ===== 修复代理节点配置 =====
proxies:
  # 修复的Hysteria2节点配置
  - name: "🇺🇸 美国-Hysteria2"
    type: hysteria2
    server: ipv6.zxh425.dpdns.org
    port: 443
    password: "3n5T8b9NhTZe5cm2NW"
    obfs: 
      type: salamander
      password: "96Jj12oLbwuEQOrZ39"
    sni: "ipv6.zxh425.dpdns.org"
    skip-cert-verify: false

  # VLESS Reality节点 (保持正确)
  - name: "🇺🇸 美国-VLESS"
    type: vless
    server: ipv6.zxh425.dpdns.org
    port: 443
    uuid: "3a7d914d-86d9-4cec-8e65-b08f5c0b98ab"
    network: tcp
    tls: true
    udp: true
    flow: "xtls-rprx-vision"
    servername: "gateway.icloud.com"
    reality-opts:
      public-key: "IXNtZnkoBPz2Yz-o1Jia8gQLeCj4mh-cszPFQys_OH4"
      short-id: "f71dda44e6f4f3b7"
    client-fingerprint: chrome

# ===== 策略组 =====
proxy-groups:
  - name: "🚀 美国节点"
    type: select
    proxies:
      - "🇺🇸 美国-Hysteria2"
      - "🇺🇸 美国-VLESS"

  - name: "🚀 自动选择"
    type: url-test
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    proxies:
      - "🇺🇸 美国-Hysteria2"
      - "🇺🇸 美国-VLESS"

  - name: "🔰 流量分流"
    type: select
    proxies:
      - "🚀 自动选择"
      - "🚀 美国节点"
      - DIRECT

# ===== DNS 防泄露核心配置 =====
dns:
  enable: true
  listen: 0.0.0.0:53
  enhanced-mode: redir-host   # 手机端兼容性更好
  nameserver:
    - https://1.1.1.1/dns-query  # Cloudflare DoH
  fallback:
    - tls://8.8.4.4:853  # Google DoT (fallback)
  fallback-filter:
    geoip: true
    geoip-code: CN

# ===== 优化规则分流 =====
rules:
  # 国内直连
  - GEOIP,CN,DIRECT
  # 国外走代理
  - MATCH,"🔰 流量分流"
    
# ===== 手机端特殊设置 =====
interface-name: "wlan*"      # 匹配手机WiFi
tun:
  enable: true
  stack: system
  dns-hijack:
    - 0.0.0.0:53
  auto-route: true
  auto-detect-interface: true # 自动选择网络接口
  
# ===== 强制DNS安全 =====
script:
  shortcuts:
    force_dns: |
      rules:
        - DST-PORT,53,🔰 流量分流
        - DST-PORT,853,🔰 流量分流
